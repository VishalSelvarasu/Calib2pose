# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# Model weights (185 MB each, over GitHub's 100 MB limit)
*.pt
*.pth
checkpoints*/

# Datasets and downloaded assets -- all regenerate from scripts + seeds
data/
debug_data/
captures/
synthetic/
assets/
*.msh
stage5_closed_loop/ur5e/

# Generated MJCF, written at runtime
_scene_gen.xml
_ur5e_cam.xml
drill_points.npy

# NOTE: results/ is NOT ignored. The JSON files are the findings the READMEs
# cite, and the figures are the visual evidence that the pipeline ran on real
# pixels. Only the bulky per-image verification dumps are excluded.
stage4_keypoints/data/verify/
debug_data/verify/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/